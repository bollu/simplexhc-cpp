# RUN: ./compile-and-run-program.sh %s | FileCheck %s
# CHECK: 4

data Int  = Int2 (PrimInt PrimInt);
binding k = \(x:Int y:Int) -> Void { printInt (x) };
binding three = \() -> Int { Int2 (3 4) };
binding main = \() -> Void { case three of
                                Int2 (x y) ->  k (y x);
                            };
