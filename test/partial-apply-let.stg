# RUN: ./compile-and-run-program.sh %s | FileCheck %s
# CHECK: 1

data IntT  = IntCons (PrimInt);

binding one = \() -> A { IntCons (1) };
binding two = \() -> A { IntCons (2) };

binding k = \(x:A y:A) -> A { x () };
binding main = \() -> Void { let 
                                binding k1 = \() -> A { k (one) };
                                binding k12 = \(k1: F) () -> A { k1 (two) };
                             in case k12 of
                                    IntCons (x) -> printInt (x);
                           };
